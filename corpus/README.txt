Law and policy corpus used by the engine (settings: corpus.path).

One passage per .md file, with a --- header (id, title, cites, version).
EU passages are SHORT SUMMARIES written for testing, not the legal text.
The lending policy passages mirror the synthetic lender's policy PL-2026.1.
The folder's content hash is recorded in every run, so a pack says which
version of which text it was checked against.
